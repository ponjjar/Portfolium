import React from 'react';
import { View } from 'react-native';

export function WebCropModal(props: any) {
  // This is a no-op fallback for Native. 
  // Native uses Expo ImagePicker's built-in allowsEditing crop UI.
  return <View />;
}
